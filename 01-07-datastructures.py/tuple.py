# Beispiel eines Tupels
coordinates = (10.0, 20.5)
print(coordinates)  # Ausgabe: (10.0, 20.5)

# Versuch, ein Element zu ändern (führt zu einem Fehler)
coordinates[0] = 5.0  # TypeError: 'tuple' object does not support item assignment
